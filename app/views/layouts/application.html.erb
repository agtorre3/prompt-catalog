<!DOCTYPE html>
<html>
  <head>
    <title>PromptCatalog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="bg-gray-800 text-white p-4 fixed w-full top-0 z-50">
      <div class="container mx-auto flex justify-between items-center">
        <div class="text-xl font-bold">
          <%= link_to "Prompt Catalog", root_path, class: "hover:text-gray-300" %>
        </div>
        <div class="flex items-center space-x-4">
          <% if user_signed_in? %>
            <span class="text-gray-300">Welcome, <%= current_user.email %></span>
            <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "bg-red-500 hover:bg-red-600 px-4 py-2 rounded" %>
          <% else %>
            <%= link_to "Sign in", new_user_session_path, class: "hover:text-gray-300" %>
            <%= link_to "Sign up", new_user_registration_path, class: "bg-blue-500 hover:bg-blue-600 px-4 py-2 rounded" %>
          <% end %>
        </div>
      </div>
    </nav>

    <% if notice %>
      <p class="notice bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative"><%= alert %></p>
    <% end %>

    <main class="container mx-auto mt-28 px-5 flex">
      <%= yield %>
    </main>
  </body>
</html>
